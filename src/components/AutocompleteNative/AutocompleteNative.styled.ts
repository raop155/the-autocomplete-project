import styled from "styled-components";

export const StyledAutocompleteNative = styled.div`
  display: block;
  width: 100%;

  & > input {
    margin-top: var(--space-sm);
    box-shadow: 2.5px 2.5px 2.5px var(--grey-3);
  }
`;
